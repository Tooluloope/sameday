import React from 'react';
import './snack.css';

export const Snack = ({animate}) => {
    return (
       
       <div className={`hamburger-menu ${ animate ? 'animate':'' }`}>

       </div>
    )
}